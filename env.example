# Database
DATABASE_URL="file:./prisma/dev.db"

# Node Environment
NODE_ENV="development"

# Production Settings (Railway)
# DATABASE_URL será configurado automaticamente pelo Railway
# NODE_ENV será "production" automaticamente

